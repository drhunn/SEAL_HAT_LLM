# Model unit runtime

## Feature family

Model unit runtime.

## Subfeatures

- unit spec
- unit registry
- local runtime bootstrap
- health and lifecycle

Use this folder for feature slices that change the one-harness-per-model runtime lane.
