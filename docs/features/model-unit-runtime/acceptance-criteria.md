# Acceptance criteria

Use this file to define the concrete checks for the model-unit-runtime slice being worked.

At minimum, state:
- which unit-runtime subfeatures are included
- what runtime/unit behavior must pass
- what is explicitly out of scope
