# Acceptance criteria

List the conditions that must be true for the feature to count as complete.

Write them as concrete checks, not aspirations.

Examples:
- config loads with the new fields
- the runtime rejects invalid input in the defined way
- the new persistence path records both success and failure cases
- the required tests pass
